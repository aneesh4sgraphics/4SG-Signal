In server/routes-leads.ts, find the POST /api/leads/:id/activities endpoint. After the call_made task creation block (around line 1280, after the closing } of the if (data.activityType === 'call_made') block), add a new block that handles mailer activities:

      // Auto-create follow-up task when a mailer is logged on a lead
      const mailerTypes = ['mailer_one_page', 'mailer_envelope', 'mailer_press_kit'];
      if (mailerTypes.includes(data.activityType)) {
        try {
          const daysUntilFollowUp = data.activityType === 'mailer_press_kit' ? 14
            : data.activityType === 'mailer_envelope' ? 10
            : 7;
          
          const dueDate = new Date();
          dueDate.setDate(dueDate.getDate() + daysUntilFollowUp);

          const mailerLabel = data.activityType === 'mailer_press_kit' ? 'Press test kit'
            : data.activityType === 'mailer_envelope' ? 'Sample envelope'
            : 'One-page mailer';

          const [lead] = await db.select({ name: leads.name, company: leads.company }).from(leads).where(eq(leads.id, leadId));
          
          await storage.createFollowUpTask({
            leadId,
            customerId: null,
            title: `${mailerLabel} sent — call to confirm receipt`,
            description: `${mailerLabel} was sent to ${lead?.company || lead?.name || 'lead'}. Call in ${daysUntilFollowUp} days to confirm they received it and ask for feedback.`,
            taskType: 'follow_up',
            priority: 'high',
            status: 'pending',
            dueDate,
            isAutoGenerated: true,
            sourceType: 'mailer_sent',
            sourceId: String(result[0].id),
            assignedTo: req.user?.id,
            assignedToName: req.user?.firstName ? `${req.user.firstName} ${req.user.lastName || ''}`.trim() : req.user?.email,
          });
        } catch (mailerTaskError) {
          console.warn('[Tasks] Failed to create mailer follow-up task:', mailerTaskError);
        }
      }

Also update the onePageMailerSentAt / sampleEnvelopeSentAt / pressTestKitSentAt fields on the lead when the respective activity is logged. After the db.update(leads) that sets lastContactAt, add:

      // Update mailer timestamp fields on the lead
      if (data.activityType === 'mailer_one_page') {
        await db.update(leads).set({ onePageMailerSentAt: new Date(), lastMailerSentAt: new Date(), lastMailerType: 'one_page' }).where(eq(leads.id, leadId));
      } else if (data.activityType === 'mailer_envelope') {
        await db.update(leads).set({ sampleEnvelopeSentAt: new Date(), lastMailerSentAt: new Date(), lastMailerType: 'envelope' }).where(eq(leads.id, leadId));
      } else if (data.activityType === 'mailer_press_kit') {
        await db.update(leads).set({ pressTestKitSentAt: new Date(), lastMailerSentAt: new Date(), lastMailerType: 'press_kit' }).where(eq(leads.id, leadId));
      }

Do not change anything else.