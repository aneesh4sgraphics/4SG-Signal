In server/routes.ts, make two changes:

CHANGE 1 — In the /api/tasks/list endpoint (around line 29146), add a "this year" filter to the baseConditions. Find the line:

  const baseConditions: any[] = [eq(followUpTasks.status, 'pending')];

Replace it with:

  const thisYearStart = new Date('2026-01-01T00:00:00.000Z');
  const baseConditions: any[] = [
    eq(followUpTasks.status, 'pending'),
    gte(followUpTasks.createdAt, thisYearStart),
  ];

CHANGE 2 — In server/gmail-sync-worker.ts, find where a drip reply is detected and a 'note' activity is logged (around the line: activityType: 'note', summary: 'Replied to drip campaign'). 

Right after that db.insert(leadActivities) call, add a follow-up task creation:

  // Auto-create follow-up task when lead replies to drip
  try {
    if (leadId) {
      const dueDate = new Date();
      dueDate.setDate(dueDate.getDate() + 1); // due tomorrow
      await db.insert(followUpTasks).values({
        leadId,
        taskType: 'follow_up',
        title: `Follow up with lead — replied to drip campaign`,
        description: `Lead replied to the "${campaignName}" drip campaign. Reach out personally.`,
        priority: 'high',
        status: 'pending',
        dueDate,
        isAutoGenerated: true,
        sourceType: 'drip_reply',
      });
    }
  } catch (taskErr) {
    console.error('[Drip Reply] Failed to create follow-up task:', taskErr);
  }

Make sure followUpTasks is imported from @shared/schema in gmail-sync-worker.ts.

Also in server/gmail-intelligence.ts, find where firstEmailReplyAt is set on a lead (around: .set({ firstEmailReplyAt: message.sentAt || new Date() })). Right after that db.update call, add:

  // Auto-create follow-up task when Gmail reply detected
  try {
    const dueDate = new Date();
    dueDate.setDate(dueDate.getDate() + 1);
    await db.insert(followUpTasks).values({
      leadId: leadByEmail[0].id,
      taskType: 'follow_up',
      title: `Follow up — lead replied to your email`,
      description: `Gmail detected a reply from this lead. Follow up personally.`,
      priority: 'high',
      status: 'pending',
      dueDate,
      isAutoGenerated: true,
      sourceType: 'gmail_reply',
    });
  } catch (taskErr) {
    console.error('[Lead Trust] Failed to create follow-up task:', taskErr);
  }

Make sure followUpTasks is imported from @shared/schema in gmail-intelligence.ts.

Do not change anything else.